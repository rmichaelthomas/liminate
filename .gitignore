__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.hypothesis/
venv/
.venv/

# PyInstaller build artifacts (binaries built locally or in CI).
# The committed build/ dir holds entry.py + build_binary.sh; PyInstaller's
# intermediate `build/<name>/` work-dir is ignored alongside dist/.
build/__pycache__/
/build/liminate/
dist/
*.spec

# macOS Finder metadata
.DS_Store

# Superpowers skill scratch / local notes — not part of the spec.
docs/superpowers/

# Branch D reference asset — kept locally for design reference, not committed.
liminate_visual_identity.html

# Local experiment scratch — exploratory work, not part of the spec.
experiments/
