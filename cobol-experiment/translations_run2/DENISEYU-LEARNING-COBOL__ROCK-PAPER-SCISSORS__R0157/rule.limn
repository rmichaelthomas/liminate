about "player-choice predicate"

remember a value called player-choice with "rock"

permit player-choice is "rock" because "COBOL IF predicate is recorded as an eligibility rule"

show "player-choice predicate evaluated."
