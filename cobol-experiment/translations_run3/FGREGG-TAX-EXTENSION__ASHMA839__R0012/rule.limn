about "prior adjusted base, rounded (ASHMA839)"

remember a value called prior-base with 1000
remember a value called prior-multiplier with 1.05
remember a value called prior-adjusted-base with prior-base multiplied by prior-multiplier because "COMPUTE PREV-ADJ-BASE ROUNDED = PREV-BASE * PREV-MULTIPLY; the literal ROUNDED directive is not expressible in the base vocabulary, so the product is carried without the half-up rounding"

show "prior adjusted base computed."
