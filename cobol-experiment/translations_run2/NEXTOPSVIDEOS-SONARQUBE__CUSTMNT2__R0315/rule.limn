about "response-code predicate"

remember a value called response-code with "DFHRESP(DUPREC)"

permit response-code is "DFHRESP(DUPREC)" because "COBOL IF predicate is recorded as an eligibility rule"

show "response-code predicate evaluated."
