about "response-code predicate"

remember a value called response-code with "DFHRESP(NORMAL)"

permit response-code is "DFHRESP(NORMAL)" because "COBOL IF predicate is recorded as an eligibility rule"

show "response-code predicate evaluated."
